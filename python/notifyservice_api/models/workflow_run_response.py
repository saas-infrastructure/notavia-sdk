# coding: utf-8

"""
    Notavia API

    White-label notification engine. Authenticate with a bearer API key issued per environment. All `/v1/*` paths require `Authorization: Bearer <api_key>`.  The OAuth install/callback routes (`/oauth/slack/*`) require cookie authentication (dashboard session) rather than an API key. 

    The version of the OpenAPI document: 1.0.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from datetime import datetime
from pydantic import BaseModel, ConfigDict, Field, StrictInt, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from notifyservice_api.models.workflow_step_run_summary import WorkflowStepRunSummary
from typing import Optional, Set
from typing_extensions import Self

class WorkflowRunResponse(BaseModel):
    """
    WorkflowRunResponse
    """ # noqa: E501
    id: StrictStr
    workflow_key: StrictStr
    version: StrictInt
    status: StrictStr
    current_step_id: Optional[StrictStr] = None
    trigger_data: StrictStr = Field(description="Raw JSON string of the trigger payload.")
    source: StrictStr
    created_at: datetime
    completed_at: Optional[datetime] = None
    failure_reason: Optional[StrictStr] = None
    steps: List[WorkflowStepRunSummary]
    __properties: ClassVar[List[str]] = ["id", "workflow_key", "version", "status", "current_step_id", "trigger_data", "source", "created_at", "completed_at", "failure_reason", "steps"]

    @field_validator('status')
    def status_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set(['pending', 'running', 'waiting', 'completed', 'failed', 'cancelling', 'cancelled']):
            raise ValueError("must be one of enum values ('pending', 'running', 'waiting', 'completed', 'failed', 'cancelling', 'cancelled')")
        return value

    @field_validator('source')
    def source_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set(['Production', 'Test']):
            raise ValueError("must be one of enum values ('Production', 'Test')")
        return value

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of WorkflowRunResponse from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of each item in steps (list)
        _items = []
        if self.steps:
            for _item_steps in self.steps:
                if _item_steps:
                    _items.append(_item_steps.to_dict())
            _dict['steps'] = _items
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of WorkflowRunResponse from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "id": obj.get("id"),
            "workflow_key": obj.get("workflow_key"),
            "version": obj.get("version"),
            "status": obj.get("status"),
            "current_step_id": obj.get("current_step_id"),
            "trigger_data": obj.get("trigger_data"),
            "source": obj.get("source"),
            "created_at": obj.get("created_at"),
            "completed_at": obj.get("completed_at"),
            "failure_reason": obj.get("failure_reason"),
            "steps": [WorkflowStepRunSummary.from_dict(_item) for _item in obj["steps"]] if obj.get("steps") is not None else None
        })
        return _obj


