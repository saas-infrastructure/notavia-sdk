# coding: utf-8

"""
    Notavia API

    White-label notification engine. Authenticate with a bearer API key issued per environment. All `/v1/*` paths require `Authorization: Bearer <api_key>`.  The OAuth install/callback routes (`/oauth/slack/*`) require cookie authentication (dashboard session) rather than an API key. 

    The version of the OpenAPI document: 1.0.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from typing import Optional, Set
from typing_extensions import Self

class Recipient(BaseModel):
    """
    Identifies the notification target. At most one typed field should be set; the server selects the correct field based on `channel`.  - **Email / SMS / In-app:** use `address` (email address, phone number E.164, or opaque external user id). - **Slack:** use `slack_user_id` (for DMs) or `slack_channel_id` (for channel posts). - **Teams / Discord:** use `endpoint` (the endpoint **key**, e.g. `\"billing-alerts\"`) or the legacy   `teams_endpoint_id` / `discord_endpoint_id` (UUID of the configured endpoint). - `external_user_id` resolves the recipient via stored preference/inbox records. - `name` is used only for email `To:` display name and in-app display. 
    """ # noqa: E501
    address: Optional[StrictStr] = None
    name: Optional[StrictStr] = None
    external_user_id: Optional[StrictStr] = None
    endpoint: Optional[StrictStr] = Field(default=None, description="The **key** of a Discord or Teams endpoint (e.g. `\"billing-alerts\"`). Preferred over the legacy UUID fields. When set, `teams_endpoint_id` / `discord_endpoint_id` are ignored. ")
    slack_user_id: Optional[StrictStr] = Field(default=None, description="Slack user id (Uxxxxxxxx) for DM delivery. Mutually exclusive with `slack_channel_id`.")
    slack_channel_id: Optional[StrictStr] = Field(default=None, description="Slack channel id (Cxxxxxxxx) for channel-post delivery. Mutually exclusive with `slack_user_id`.")
    teams_endpoint_id: Optional[StrictStr] = Field(default=None, description="UUID of the `TeamsWebhookEndpoint` to send to. Legacy — prefer `endpoint` (the key).")
    discord_endpoint_id: Optional[StrictStr] = Field(default=None, description="UUID of the `DiscordWebhookEndpoint` to send to. Legacy — prefer `endpoint` (the key).")
    __properties: ClassVar[List[str]] = ["address", "name", "external_user_id", "endpoint", "slack_user_id", "slack_channel_id", "teams_endpoint_id", "discord_endpoint_id"]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of Recipient from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of Recipient from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "address": obj.get("address"),
            "name": obj.get("name"),
            "external_user_id": obj.get("external_user_id"),
            "endpoint": obj.get("endpoint"),
            "slack_user_id": obj.get("slack_user_id"),
            "slack_channel_id": obj.get("slack_channel_id"),
            "teams_endpoint_id": obj.get("teams_endpoint_id"),
            "discord_endpoint_id": obj.get("discord_endpoint_id")
        })
        return _obj


