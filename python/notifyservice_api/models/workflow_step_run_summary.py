# coding: utf-8

"""
    Notavia API

    White-label notification engine. Authenticate with a bearer API key issued per environment. All `/v1/*` paths require `Authorization: Bearer <api_key>`.  The OAuth install/callback routes (`/oauth/slack/*`) require cookie authentication (dashboard session) rather than an API key. 

    The version of the OpenAPI document: 1.0.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from datetime import datetime
from pydantic import BaseModel, ConfigDict, Field, StrictInt, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from typing import Optional, Set
from typing_extensions import Self

class WorkflowStepRunSummary(BaseModel):
    """
    WorkflowStepRunSummary
    """ # noqa: E501
    step_id: StrictStr
    kind: StrictStr
    attempt_id: StrictInt
    status: StrictStr
    outcome: Optional[StrictStr] = None
    notification_id: Optional[StrictStr] = None
    output_json: Optional[StrictStr] = Field(default=None, description="JSON snapshot of the step output captured at completion.")
    next_step_id: Optional[StrictStr] = None
    next_scheduled_at: Optional[datetime] = None
    started_at: datetime
    completed_at: Optional[datetime] = None
    __properties: ClassVar[List[str]] = ["step_id", "kind", "attempt_id", "status", "outcome", "notification_id", "output_json", "next_step_id", "next_scheduled_at", "started_at", "completed_at"]

    @field_validator('kind')
    def kind_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set(['send', 'delay', 'branch', 'digest', 'wait_for_event', 'cancel', 'noop']):
            raise ValueError("must be one of enum values ('send', 'delay', 'branch', 'digest', 'wait_for_event', 'cancel', 'noop')")
        return value

    @field_validator('status')
    def status_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set(['running', 'waiting', 'completed', 'failed', 'cancelled']):
            raise ValueError("must be one of enum values ('running', 'waiting', 'completed', 'failed', 'cancelled')")
        return value

    @field_validator('outcome')
    def outcome_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['sent', 'suppressed', 'branched', 'delayed', 'event_received', 'timed_out', 'failed', 'cancelled', 'noop']):
            raise ValueError("must be one of enum values ('sent', 'suppressed', 'branched', 'delayed', 'event_received', 'timed_out', 'failed', 'cancelled', 'noop')")
        return value

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of WorkflowStepRunSummary from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of WorkflowStepRunSummary from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "step_id": obj.get("step_id"),
            "kind": obj.get("kind"),
            "attempt_id": obj.get("attempt_id"),
            "status": obj.get("status"),
            "outcome": obj.get("outcome"),
            "notification_id": obj.get("notification_id"),
            "output_json": obj.get("output_json"),
            "next_step_id": obj.get("next_step_id"),
            "next_scheduled_at": obj.get("next_scheduled_at"),
            "started_at": obj.get("started_at"),
            "completed_at": obj.get("completed_at")
        })
        return _obj


