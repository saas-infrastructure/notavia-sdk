# coding: utf-8

"""
    Notavia API

    White-label notification engine. Authenticate with a bearer API key issued per environment. All `/v1/*` paths require `Authorization: Bearer <api_key>`.  The OAuth install/callback routes (`/oauth/slack/*`) require cookie authentication (dashboard session) rather than an API key. 

    The version of the OpenAPI document: 1.0.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from datetime import datetime
from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictInt, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from typing import Optional, Set
from typing_extensions import Self

class UsageResponse(BaseModel):
    """
    Current-period live usage and resolved plan limits.
    """ # noqa: E501
    plan: StrictStr = Field(description="Resolved plan key (e.g. `free`, `oem`).")
    period_start: datetime = Field(description="Inclusive UTC start of the current billing period (first of the month).")
    period_end: datetime = Field(description="Exclusive UTC end of the current billing period (first of the next month).")
    included_sends: Optional[StrictInt] = Field(default=None, description="Monthly included send allowance, or null for unmetered plans.")
    used_sends: StrictInt = Field(description="Live (non-test) sends consumed in the current period.")
    remaining_sends: Optional[StrictInt] = Field(default=None, description="Sends remaining before the cap, or null when there is no cap.")
    hard_limit: StrictBool = Field(description="When true, sends are rejected once the included allowance is exhausted.")
    by_channel: Dict[str, StrictInt] = Field(description="Live sends consumed in the current period, keyed by lowercase channel name.")
    __properties: ClassVar[List[str]] = ["plan", "period_start", "period_end", "included_sends", "used_sends", "remaining_sends", "hard_limit", "by_channel"]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of UsageResponse from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of UsageResponse from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "plan": obj.get("plan"),
            "period_start": obj.get("period_start"),
            "period_end": obj.get("period_end"),
            "included_sends": obj.get("included_sends"),
            "used_sends": obj.get("used_sends"),
            "remaining_sends": obj.get("remaining_sends"),
            "hard_limit": obj.get("hard_limit"),
            "by_channel": obj.get("by_channel")
        })
        return _obj


